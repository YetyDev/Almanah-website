<footer>
  <div class="container">
    <div class="footer-grid">
      
      <div class="footer-column footer-about">
        <div class="logo-container">
          <img src="/logo.jpeg" alt="Almanah Foundation Logo">
          <div class="logo-text">
            Almanah
            <span>Love & Care Foundation</span>
          </div>
        </div>
        <p>
          We empower widows, support young girls, and educate children to establish independent households and sustainable communities.
        </p>
        <div style="display: flex; align-items: center; gap: 0.75rem; margin-bottom: 2rem; font-size: 0.95rem; color: rgba(255, 255, 255, 0.8);">
          <svg viewBox="0 0 24 24" style="width: 18px; height: 18px; fill: var(--accent); flex-shrink: 0;">
            <path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/>
          </svg>
          <span><a href="mailto:info@almanahcarefoundation.org" style="color: var(--accent); font-weight: 600; transition: var(--transition);">info@almanahcarefoundation.org</a></span>
        </div>
        <div class="social-links">
          <a href="https://www.facebook.com/share/1FUR9BJtWo/?mibextid=wwXIfr" class="social-link" aria-label="Facebook" target="_blank" rel="noopener noreferrer">
            <svg viewBox="0 0 24 24"><path d="M5 3h14a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2m13 2h-2.5A3.5 3.5 0 0 0 12 8.5V11h-2v3h2v7h3v-7h3v-3h-3V9a1 1 0 0 1 1-1h2V5z"/></svg>
          </a>
          <a href="#" class="social-link" aria-label="Instagram">
            <svg viewBox="0 0 24 24"><path d="M7.8 2h8.4C19.4 2 22 4.6 22 7.8v8.4a5.8 5.8 0 0 1-5.8 5.8H7.8C4.6 22 2 19.4 2 16.2V7.8A5.8 5.8 0 0 1 7.8 2m-.8 2a3.8 3.8 0 0 0-3.8 3.8v8.4c0 2.1 1.7 3.8 3.8 3.8h8.4c2.1 0 3.8-1.7 3.8-3.8V7.8c0-2.1-1.7-3.8-3.8-3.8H7m8.5 2.1c.5 0 .9.4.9.9s-.4.9-.9.9-.9-.4-.9-.9.4-.9.9-.9M12 6.5a5.5 5.5 0 1 1 0 11 5.5 5.5 0 0 1 0-11m0 2a3.5 3.5 0 1 0 0 7 3.5 3.5 0 0 0 0-7z"/></svg>
          </a>
          <a href="#" class="social-link" aria-label="Twitter">
            <svg viewBox="0 0 24 24"><path d="M22.46 6c-.77.35-1.6.58-2.46.69.88-.53 1.56-1.37 1.88-2.38-.83.5-1.75.85-2.72 1.05C18.37 4.5 17.26 4 16 4c-2.35 0-4.27 1.92-4.27 4.29 0 .34.04.67.11.98C8.28 9.09 5.11 7.38 3 4.79c-.37.63-.58 1.37-.58 2.15 0 1.48.75 2.78 1.9 3.55-.7 0-1.35-.2-1.93-.53v.05c0 2.05 1.46 3.76 3.4 4.15-.36.1-.73.15-1.12.15-.27 0-.54-.03-.8-.08.54 1.68 2.1 2.9 3.95 2.94-1.44 1.13-3.26 1.8-5.23 1.8-.34 0-.67-.02-1-.06C2.62 19.39 4.8 20 7.14 20c7.81 0 12.08-6.47 12.08-12.08 0-.18 0-.37-.01-.55.83-.6 1.54-1.35 2.11-2.2-.76.34-1.58.57-2.44.67.87-.52 1.54-1.35 1.85-2.34z"/></svg>
          </a>
        </div>
      </div>

      <div class="footer-column">
        <h4>Quick Links</h4>
        <ul class="footer-links">
          <li><a href="/">Home</a></li>
          <li><a href="/about">About Us</a></li>
          <li><a href="/programs">Our Programs</a></li>
          <li><a href="/events">Events</a></li>
          <li><a href="/impact">Our Impact</a></li>
          <li><a href="/contact">Contact Us</a></li>
        </ul>
      </div>

      <div class="footer-column">
        <h4>Our Programs</h4>
        <ul class="footer-links">
          <li><a href="/programs#widow-empowerment">Widow Empowerment</a></li>
          <li><a href="/programs#girl-mentorship">Girl Mentorship</a></li>
          <li><a href="/programs#child-scholarships">Child Scholarships</a></li>
          <li><a href="/programs#community-relief">Community Relief</a></li>
        </ul>
      </div>

      <div class="footer-column">
        <h4>Newsletter</h4>
        <p style="color: rgba(255,255,255,0.6); font-size: 0.9rem; margin-bottom: 1rem;">
          Subscribe to receive reports, success stories, and campaign updates.
        </p>
        <form class="newsletter-form" id="newsletter-subscription-form">
          <input type="email" class="newsletter-input" placeholder="Your Email Address" required id="newsletter-email">
          <button type="submit" class="btn btn-secondary btn-subscribe">Subscribe</button>
        </form>
      </div>

    </div>

    <div class="footer-bottom">
      <p>© 2026 Almanah Love and Care Foundation. All Rights Reserved.</p>
      <div class="footer-bottom-links">
        <a href="/privacy">Privacy Policy</a>
        <a href="/terms">Terms of Use</a>
      </div>
    </div>
  </div>
</footer>

<!-- CUSTOM SUCCESS MODAL/BANNER -->
<div id="alert-banner" style="position: fixed; bottom: 30px; right: 30px; background: var(--primary); color: white; padding: 1.25rem 2rem; border-radius: var(--border-radius); box-shadow: var(--shadow-lg); z-index: 2000; transform: translateY(150%); opacity: 0; transition: var(--transition); display: flex; align-items: center; gap: 1rem;">
  <svg viewBox="0 0 24 24" style="width: 24px; height: 24px; fill: var(--accent);">
    <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z" />
  </svg>
  <div style="font-family: var(--font-title); font-weight: 600;" id="alert-message">Thank you for your support!</div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    
    // 1. MOBILE NAVBAR DRAWER TOGGLE
    const mobileToggle = document.getElementById('mobile-toggle');
    const navbar = document.getElementById('navbar');

    if (mobileToggle) {
      mobileToggle.addEventListener('click', () => {
        const isActive = navbar.classList.toggle('active');
        mobileToggle.classList.toggle('active', isActive);
      });
    }

    // Close menu when links are clicked on mobile
    const navLinks = document.querySelectorAll('nav a');
    navLinks.forEach(link => {
      link.addEventListener('click', (e) => {
        if (navbar.classList.contains('active')) {
          navbar.classList.remove('active');
          if (mobileToggle) {
            mobileToggle.classList.remove('active');
          }
        }
      });
    });

    // 2. HEADER SCROLL SHRINK EFFECT
    const mainHeader = document.getElementById('main-header');
    if (mainHeader) {
      const handleScroll = () => {
        if (window.scrollY > 20) {
          mainHeader.classList.add('header-scrolled');
        } else {
          mainHeader.classList.remove('header-scrolled');
        }
      };
      
      // Run once on load to set initial state if page is already scrolled
      handleScroll();
      window.addEventListener('scroll', handleScroll);
    }

    // 2. SUCCESS ALERT SYSTEM
    const showSuccessAlert = (message) => {
      const banner = document.getElementById('alert-banner');
      const bannerMsg = document.getElementById('alert-message');
      bannerMsg.innerText = message;
      banner.style.transform = 'translateY(0)';
      banner.style.opacity = '1';
      
      setTimeout(() => {
        banner.style.transform = 'translateY(150%)';
        banner.style.opacity = '0';
      }, 5000);
    };

    window.showSuccessAlert = showSuccessAlert; // Make globally accessible

    // 3. NEWSLETTER FORM SUBMISSION
    const newsletterForm = document.getElementById('newsletter-subscription-form');
    if (newsletterForm) {
      newsletterForm.addEventListener('submit', (e) => {
        e.preventDefault();
        const email = document.getElementById('newsletter-email').value;
        
        fetch('/api/newsletter', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({ email })
        })
        .then(res => res.json())
        .then(data => {
          if (data.success) {
            showSuccessAlert(data.message);
            newsletterForm.reset();
          } else {
            alert(data.error || 'Something went wrong.');
          }
        })
        .catch(err => {
          console.error('Newsletter error:', err);
          alert('An error occurred. Please try again.');
        });
      });
    }

    // 4. BACK TO TOP BUTTON LOGIC
    const backToTopBtn = document.getElementById('back-to-top');
    if (backToTopBtn) {
      window.addEventListener('scroll', () => {
        if (window.scrollY > 300) {
          backToTopBtn.classList.add('visible');
        } else {
          backToTopBtn.classList.remove('visible');
        }
      });
      
      backToTopBtn.addEventListener('click', () => {
        window.scrollTo({
          top: 0,
          behavior: 'smooth'
        });
      });
    }

  });
</script>

<%- include('cookie-consent') %>

<!-- Back to Top Button -->
<button id="back-to-top" class="back-to-top-btn" aria-label="Back to top">
  <svg viewBox="0 0 24 24">
    <path d="M7.41 15.41L12 10.83l4.59 4.58L18 14l-6-6-6 6z" />
  </svg>
</button>
</body>
</html>
