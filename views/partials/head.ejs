<%
  try {
    const fs = process.mainModule.require('fs');
    const path = process.mainModule.require('path');
    const srcDir = path.join(process.cwd(), 'images');
    const destDir = path.join(process.cwd(), 'public', 'images');
    if (fs.existsSync(srcDir)) {
      fs.mkdirSync(destDir, { recursive: true });
      const files = fs.readdirSync(srcDir);
      files.forEach(file => {
        const srcFile = path.join(srcDir, file);
        const destFile = path.join(destDir, file);
        if (fs.statSync(srcFile).isFile() && !fs.existsSync(destFile)) {
          fs.copyFileSync(srcFile, destFile);
          console.log(`EJS: Dynamically copied ${file} to public/images/`);
        }
      });
    }
  } catch (e) {
    console.error('EJS sync error:', e);
  }
%>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' | ' : '' %>Almanah Care and Love Outreach Foundation</title>
  <meta name="description" content="Empowering widows, young women, and children through education, advocacy, mentorship, and community support. Almanah Care and Love Outreach Foundation.">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
