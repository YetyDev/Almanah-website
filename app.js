// This file serves as an entry point redirecting to server.js.
// It is useful for hosting environments (like cPanel, Passenger, or Vercel)
// that expect 'app.js' as the default startup/entry file.

require('./server.js');
